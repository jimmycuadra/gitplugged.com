<%= form_for(@repo) do |f| %>
  <% if @repo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@repo.errors.count, "error") %> prohibited this repo from being saved:</h2>

      <ul>
      <% @repo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :vote_sum %><br />
    <%= f.number_field :vote_sum %>
  </div>
  <div class="field">
    <%= f.label :week_start %><br />
    <%= f.date_select :week_start %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
